# 社内ダッシュボード アーキテクチャ設計

## 1. 技術スタック

| 項目 | 技術 |
|----|----|
| フロントエンド | HTML / CSS / JavaScript |
| バックエンド | Google Apps Script |
| データ管理 | Google スプレッドシート |
| 画像管理 | Google Drive |
| ポータル | Googleサイト |

---

## 2. 全体構成

```
Google Sites
 └─ GAS Web App（Dashboard）
     ├─ HTML（UI）
     ├─ CSS（レイアウト）
     ├─ JS（描画制御）
     └─ Spreadsheet（APP_MASTER）
```

---

## 3. GAS Webアプリ構成

### 3.1 エントリーポイント
- `doGet(e)`
  - スプレッドシートから設定・マスタを取得
  - UI描画用データを整形
  - HTMLテンプレートへ渡す

### 3.2 データ取得仕様
- 対象行条件：
  - `enabled = TRUE`
  - `status = OK`
- 並び順：
  - `sort` 昇順

---

## 4. スプレッドシート設計思想

### 4.1 なぜスプレッドシートか
- 非エンジニアでも編集可能
- コード修正不要で運用変更できる
- Google Workspace 標準で完結

### 4.2 key の役割
- 各アプリを一意に識別
- GAS内部での安定参照用
- 表示名変更の影響を受けない

---

## 5. フロントエンド設計

### 5.1 レイアウト
- CSS Grid を使用
- カード単位でレイアウト制御

```css
.card.small { grid-column: span 1; }
.card.wide  { grid-column: span 2; }
.card.tall  { grid-row: span 2; }
```

### 5.2 レスポンシブ
- 画面幅に応じて自動折返し
- スマートフォンでも操作可能

---

## 6. 画像表示仕様

### 6.1 Drive画像
- スプレッドシートにファイルIDを保持
- GAS側で画像URL生成

### 6.2 権限
- Webアプリ実行ユーザー＝作成者
- 画像はドメイン内閲覧可推奨

---

## 7. セキュリティ・権限

| 項目 | 方針 |
|----|----|
| Webアプリ公開 | ドメイン内限定 |
| データ更新 | 管理者のみ |
| 個人情報 | 取り扱わない |

---

## 8. 拡張ポイント

- カテゴリ折りたたみ
- 検索機能
- アクセスログ保存
- 部署別表示（将来）
