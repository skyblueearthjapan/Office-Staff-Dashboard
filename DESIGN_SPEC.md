# 社内ダッシュボードアプリケーション 設計仕様書

## 1. 概要

本アプリケーションは、社内で利用されている複数の業務アプリケーション
（会議室予約、出張管理、車両予約、生産工程表、社長予定 等）への導線を
一元化する **社内共通ダッシュボード** である。

Google Workspace（GAS / Googleスプレッドシート / Googleサイト）を基盤とし、
管理性・拡張性・運用容易性を重視した構成とする。

---

## 2. 利用対象・前提条件

### 2.1 利用対象
- 全社員共通
- 部署別・ユーザー別の出し分けは行わない（初期要件）

### 2.2 利用環境
- Google Workspace
- ブラウザ（PC / タブレット / スマートフォン）

---

## 3. 全体構成

```
[Googleサイト（社内ポータル）]
           ↓
[GAS Webアプリ：社内ダッシュボード]
           ↓
[各業務アプリ（GAS / 他Webアプリ）]
```

### 3.1 役割分担
| 要素 | 役割 |
|----|----|
| Googleサイト | 社内ポータル（入口） |
| GAS Webアプリ | ダッシュボードUI |
| スプレッドシート | アプリ情報マスタ |
| Google Drive | アイコン画像保管 |

---

## 4. 画面仕様（UI/UX）

### 4.1 画面構成

#### ヘッダー
- 左：`社内ポータルへ` ボタン
- 右：ダッシュボードタイトル（例：LINEWORKS 事務所ダッシュボード）
- 装飾：波線等（CSS or SVG）

#### メインコンテンツ
- アプリボタンをカード形式で表示
- レイアウトは以下3種を想定

| layout | 説明 |
|-----|-----|
| small | 正方形（会議室予約等） |
| wide | 横長（生産工程表） |
| tall | 縦長（社長予定） |

※ CSS Grid を使用しレスポンシブ対応

### 4.2 UIデザイン方針

本ダッシュボードは、やさしいグリーンを基調とし、
アクセントとしてオレンジの流線を用いる。

視認性・安心感・社内業務ツールとしての信頼性を保ちつつ、
堅すぎないモダンなUIを目指す。

詳細は `UI_DESIGN_SPEC.md` を参照。

---

## 5. ナビゲーション仕様

### 5.1 ダッシュボード → 各アプリ
- 別タブで開く（デフォルト）
- `target="_blank" rel="noopener"`

### 5.2 各アプリ → 戻り導線（推奨）
- 「← ダッシュボードへ」
- 「🏠 社内ポータルへ」

※ 各アプリに共通ヘッダーとして実装推奨

---

## 6. データ設計（スプレッドシート）

### 6.1 マスタシート構成

#### APP_MASTER（メイン）

| 列名 | 説明 |
|----|----|
| enabled | 表示可否（TRUE/FALSE） |
| sort | 表示順 |
| key | 一意キー（GAS参照用） |
| category | カテゴリ名 |
| label | ボタン表示名 |
| url | 遷移先URL |
| layout | small / wide / tall |
| icon_source | drive_file_id / image_url / none |
| icon_value | ファイルID or URL |
| open_mode | new_tab / same_tab |
| note | 管理用メモ |
| status | 自動判定（OK / エラー） |

### 6.2 設計思想
- **UIはコードで固定しない**
- 並び替え・追加・無効化はシート編集のみで完結
- `status=OK` の行のみを表示対象とする

---

## 7. GAS Webアプリ設計

### 7.1 処理フロー

1. `doGet()`
2. SETTINGS / APP_MASTER 読み込み
3. enabled & status=OK 行を抽出
4. sort 順で整列
5. HTMLテンプレートにデータ渡し
6. ダッシュボード描画

### 7.2 HTML構成（例）

```
/html
├─ index.html
├─ style.css
└─ script.js
```

### 7.3 CSS設計方針
- CSS Grid 使用
- レイアウトクラスで形状制御
  - `.card.small`
  - `.card.wide`
  - `.card.tall`

---

## 8. 画像（アイコン）管理

### 8.1 管理方法
- Google Drive に画像保存
- スプレッドシートには **ファイルIDのみ** を保持

### 8.2 表示方法
- Webアプリ側で画像URL生成
- 権限は「ドメイン内閲覧可」推奨

---

## 9. 権限・公開設定

### 9.1 Webアプリ公開設定（推奨）
- 実行ユーザー：作成者
- アクセス権：ドメイン内全員

### 9.2 理由
- Drive画像の表示トラブルを回避
- 権限管理をシンプルに保つ

---

## 10. 運用ルール

### 10.1 日常運用
- アプリ追加：APP_MASTER に1行追加
- 一時非表示：enabled=FALSE
- URL変更：該当セル編集のみ

### 10.2 拡張余地（将来）
- カテゴリ折りたたみ
- 検索機能
- アクセスログ取得
- お気に入り登録

---

## 11. 非機能要件

- 高可用性：Google Workspace 基盤に依存
- 保守性：コード変更なしで運用変更可能
- 拡張性：列追加・UI拡張が容易

---

## 12. 本設計の位置づけ

本設計は、
- 実装エージェントへの指示書
- 将来の保守・引き継ぎ資料
- 社内標準ダッシュボードの基盤

として利用することを想定する。
